# TODO / الحدود المعروفة للمشروع

توثيق شفّاف لكل ما لم يُنفَّذ بعد أو يحتاج عملًا إضافيًا، كما طُلب صراحة (لا إخفاء لأي نقص).

## ⚠️ لم يُنفَّذ بالكامل بعد

1. **طبقة الذكاء الاصطناعي (MedBook AI)** — بطلب صريح من المستخدم أثناء البناء، تم **تأجيل** بناء هذه الميزة بالكامل (لم يُكتب أي كود لها). عند الطلب لاحقًا، النقاط الجاهزة لتسهيل إضافتها:
   - جداول `ai_conversations` و`ai_messages` موجودة بالفعل في `prisma/schema.prisma`.
   - يُنصح بإنشاء `modules/ai/` بنفس نمط بقية الوحدات، مع واجهة `AiProvider` قابلة للتبديل (`mock` افتراضيًا، ثم `anthropic`/`openai` عبر `AI_API_KEY` في `.env`) تمامًا كما هو موصوف في الطلب الأصلي.

2. **قنوات الإشعارات الخارجية (Email / SMS / WhatsApp / Push)** — مُنفَّذ حاليًا In-App فقط (جدول `notifications` + API). الكود مصمم بنمط "adapter" بسيط (`createNotification` في `notifications.service.ts`) بحيث يسهل إضافة قنوات جديدة دون تعديل بقية النظام.

3. **رفع الملفات الفعلي (صور الأطباء، شهاداتهم `doctor_documents`)** — الجداول والحقول (`photoUrl`, `DoctorDocument`) جاهزة في قاعدة البيانات، لكن لا يوجد endpoint لرفع الملفات (يحتاج تخزينًا مثل S3/Cloudinary). حاليًا `photoUrl` نص (رابط) يُدخل يدويًا فقط.

4. **الاختبار الفعلي للمشروع (تشغيل/تنفيذ)** — بيئة العمل التي بُني بها هذا المشروع **لم تملك صلاحية تشغيل Shell/npm/Docker/PostgreSQL** على هذا الجهاز. بمعنى:
   - لم يُنفَّذ `npm install` فعليًا، ولا تم التأكد من عدم وجود أخطاء Type/Build عبر مُصرّف حقيقي.
   - لم تُشغَّل الاختبارات (`npm test`) فعليًا.
   - لم يُشغَّل `docker compose up` للتأكد من نجاح كل الحاويات.
   - **الرجاء تشغيل الخطوات في `README.md` وإخباري بأي خطأ يظهر — سأصلحه فورًا.**

5. **اختبار التكامل الكامل (Integration Tests)** — `tests/integration/booking.integration.test.ts` هيكل جاهز لكنه يحتاج قاعدة بيانات اختبار حقيقية (`TEST_DATABASE_URL`) وseed مخصص لها لتنفيذ سيناريو الحجز الكامل من طرف إلى طرف؛ حاليًا يحتوي فحص "smoke test" فقط (أن `createApp()` لا يرمي استثناء).

6. **حساب "أقرب موعد متاح" في قائمة الأطباء** — البند 5 من الطلب الأصلي يذكر عرض "أقرب موعد متاح" على بطاقة كل طبيب في صفحة القائمة. حاليًا هذا الحساب موجود فقط عبر endpoint منفصل (`GET /api/doctors/:id/availability?date=`) يُستدعى داخل صفحة ملف الطبيب، وليس محسوبًا مسبقًا لكل طبيب في نتائج البحث (لتفادي بطء الاستعلام على عدد كبير من الأطباء دفعة واحدة). يمكن إضافته لاحقًا عبر job دوري يحسب `nextAvailableSlot` ويخزّنه على جدول `Doctor`.

7. **صفحة "العيادات" المستقلة** — مذكورة في رابط Header (`العيادات` كان مطلوبًا ضمن روابط الصفحة الرئيسية) لكن تم استبدالها بربطها ضمن فلترة الأطباء بدل صفحة قائمة عيادات مستقلة؛ يمكن إضافتها بسهولة (نموذج `Clinic` جاهز في قاعدة البيانات و`GET /api/admin/*` نمط مشابه يمكن تعميمه على مسار عام `/api/clinics`).

## ✅ ما تم تنفيذه بالكامل وفعليًا

- مخطط قاعدة بيانات كامل (18 نموذج) بكل الجداول المطلوبة + علاقات وindexes صحيحة.
- Auth كامل: تسجيل مريض/طبيب، تسجيل دخول، JWT (access + refresh مع rotation)، RBAC.
- نظام حجز مواعيد مع منع Double Booking على 3 مستويات (منطقي + استعلام مسبق + قيد فريد بقاعدة البيانات).
- إدارة أوقات عمل الطبيب (جدول أسبوعي + استثناءات/إجازات).
- حالات الموعد الخمس وقواعد الانتقال بينها حسب الدور (مُختبرة بوحدات اختبار).
- نظام تقييمات مع تحقق من الأهلية (فقط بعد موعد COMPLETED، مرة واحدة لكل موعد).
- إشعارات داخل التطبيق لكل الأحداث المهمة.
- لوحات تحكم كاملة وفعّالة لثلاثة الأدوار (مريض / طبيب / إدارة) بكل الوظائف المطلوبة تقريبًا.
- Seed بـ 20 طبيبًا و10 مرضى وعيادات وتخصصات وولايات جزائرية حقيقية.
- واجهة عربية RTL كاملة، متجاوبة، بنظام تصميم موحّد (Button, Input, Modal, Tabs, Badge, Pagination...).
- أمان: bcrypt، JWT، Helmet، CORS، Rate Limiting، Zod validation، RBAC، لا أسرار في الكود.
